[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "detector-based-hinf-filtering-mjls"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "cvxpy",
    "mosek",
]

[tool.setuptools.packages.find]
where = ["src"]
